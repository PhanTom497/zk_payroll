// Test Scenario: Issue 400 credit salary to Alice
//
// PREREQUISITE: 
// - Initialize payroll (get AdminCap, SpentRecord)
// - Create ticket for Alice (get RecipientTicket)
//
// EXPECTED BEHAVIOR:
// - Verifies all security checks (caller binding, payroll_id consistency)
// - Creates SalaryRecord(400 credits) owned by Alice
// - Updates SpentRecord: total_spent = 0 + 400 = 400
// - Finalize verifies: 400 <= 1000 budget ✓
//
// PRIVACY: 
// - salary_amount (400) remains encrypted
// - Alice's address only in record ownership

[issue_salary]
admin_cap: AdminCap = AdminCap {
    owner: aleo1admin...,  // Replace with actual admin address
    payroll_id: 1field
};
spent_record: SpentRecord = SpentRecord {
    owner: aleo1admin...,  // Replace with actual admin address
    total_spent: 0u64,
    payroll_id: 1field
};
recipient_ticket: RecipientTicket = RecipientTicket {
    owner: aleo1alice...,  // Replace with Alice's address
    payroll_id: 1field
};
salary_amount: u64 = 400u64;
payment_id: field = 100field;
